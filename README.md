# finiata_Task
NodeJs should be installed already.<br/>
Pull it to your local directory. <br/>
Open the folder where you pull in your IDE or Go to that direccotry through cmd. <br/>
Run the command 'npm init -y'  <br/>
Run the command 'npm install codeceptjs'  <br/>
Run the command 'npx codeceptjs run --verbose' <br/>
