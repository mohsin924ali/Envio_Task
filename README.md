# finiata_Task
NodeJs should be installed already.
Pull it to your local directory.
Open the 'test' folder in your IDE or Go to the test direccotry through cmd.
Run the command 'npx codeceptjs run --verbose'
